# 身材分析判斷邏輯修復

## 🐛 問題描述

**用戶反饋**：輸入標準的沙漏型身材數據，但系統判斷為酪梨形身材。

**原因分析**：
1. **判斷順序錯誤**：梨型判斷優先於沙漏型
2. **標準過於嚴格**：沙漏型要求胸圍-腰圍 = 18-20 cm，臀圍-腰圍 = 23-25 cm（範圍太窄）
3. **邏輯衝突**：沙漏型身材通常臀圍會比肩寬×2大，但這會先被判定為梨型

## ✅ 修復方案

### 1. 調整判斷順序

**修改前**：
```
1. 蘋果型
2. 梨型 ← 優先判斷
3. 沙漏型
4. 倒三角
5. H型
```

**修改後**：
```
1. 蘋果型
2. 沙漏型 ← 優先判斷（避免被誤判為梨型）
3. 梨型
4. 倒三角
5. H型
```

### 2. 放寬沙漏型標準

**修改前（過於嚴格）**：
```javascript
// 胸圍-腰圍 = 18-20 cm
// 臀圍-腰圍 = 23-25 cm
const isHourglass = (diffBW >= 18 && diffBW <= 20) && 
                    (diffHW >= 23 && diffHW <= 25);
```

**修改後（更合理）**：
```javascript
// 胸圍-腰圍 = 15-25 cm（放寬範圍）
// 臀圍-腰圍 = 20-30 cm（放寬範圍）
// 胸臀差 < 5 cm（確保比例均衡）
const isHourglassRelaxed = (diffBW >= 15 && diffBW <= 25) && 
                            (diffHW >= 20 && diffHW <= 30) && 
                            (diffBH <= 5);
```

### 3. 調整梨型判斷條件

**修改前**：
```javascript
// 只要臀圍 - 肩寬×2 > 3 就判定為梨型
if (diffHS > 3) {
  return { type: '梨型身材' };
}
```

**修改後**：
```javascript
// 需要同時滿足兩個條件：
// 1. 臀圍 - 肩寬×2 > 5（提高門檻）
// 2. 臀圍 > 胸圍 + 3（確保下半身明顯大於上半身）
if (diffHS > 5 && H > B + 3) {
  return { type: '梨型身材' };
}
```

### 4. 新增除錯日誌

```javascript
console.log('🔍 身材判斷數據:', {
  胸圍: B, 腰圍: W, 臀圍: H, 肩寬: S, 肩寬x2: S2.toFixed(1),
  '胸圍-腰圍': diffBW.toFixed(1),
  '臀圍-腰圍': diffHW.toFixed(1),
  '臀圍-肩寬x2': diffHS.toFixed(1),
  '肩寬x2-臀圍': diffSH.toFixed(1),
  '胸臀差': diffBH.toFixed(1)
});
```

## 📊 完整判斷邏輯

### 女性身材判斷（修復後）

```javascript
function getFemaleBodyType({ bust, waist, hips, shoulder }) {
  const B = N(bust), W = N(waist), H = N(hips), S = N(shoulder);
  const S2 = S * 2;
  
  const diffBW = B - W;  // 胸圍 - 腰圍
  const diffHW = H - W;  // 臀圍 - 腰圍
  const diffHS = H - S2; // 臀圍 - 肩寬×2
  const diffSH = S2 - H; // 肩寬×2 - 臀圍
  const diffBH = Math.abs(B - H); // 胸臀差

  // 1. 蘋果型：腰圍 > 臀圍
  if (W > H) {
    return { type: '蘋果型身材' };
  }

  // 2. 沙漏型（優先判斷）
  // 條件：胸圍-腰圍 15-25 cm，臀圍-腰圍 20-30 cm，胸臀差 < 5 cm
  const isHourglassRelaxed = (diffBW >= 15 && diffBW <= 25) && 
                              (diffHW >= 20 && diffHW <= 30) && 
                              (diffBH <= 5);
  if (isHourglassRelaxed) {
    return { type: '沙漏型身材' };
  }

  // 3. 梨型
  // 條件：臀圍 - 肩寬×2 > 5 且 臀圍 > 胸圍 + 3
  if (diffHS > 5 && H > B + 3) {
    return { type: '梨型身材（酪梨身材）' };
  }

  // 4. 倒三角：肩寬×2 - 臀圍 > 5
  if (diffSH > 5) {
    return { type: '倒三角身材' };
  }

  // 5. H型：胸圍-腰圍 < 15 或 臀圍-腰圍 < 20
  if (diffBW < 15 || diffHW < 20) {
    return { type: 'H 型身材（矩形身材）' };
  }

  return { type: '未分類' };
}
```

## 🧪 測試案例

### 案例 1：標準沙漏型

**輸入數據**：
- 胸圍：90 cm
- 腰圍：65 cm
- 臀圍：90 cm
- 肩寬：40 cm

**計算結果**：
- 胸圍-腰圍 = 25 cm ✅（15-25 範圍內）
- 臀圍-腰圍 = 25 cm ✅（20-30 範圍內）
- 胸臀差 = 0 cm ✅（< 5 cm）
- 臀圍-肩寬×2 = 10 cm（不會被判定為梨型，因為胸臀差 < 5）

**判定結果**：✅ **沙漏型身材**

### 案例 2：梨型身材

**輸入數據**：
- 胸圍：85 cm
- 腰圍：68 cm
- 臀圍：96 cm
- 肩寬：38 cm

**計算結果**：
- 胸圍-腰圍 = 17 cm
- 臀圍-腰圍 = 28 cm
- 胸臀差 = 11 cm ❌（> 5 cm，不符合沙漏型）
- 臀圍-肩寬×2 = 20 cm ✅（> 5 cm）
- 臀圍 > 胸圍 + 3 ✅（96 > 88）

**判定結果**：✅ **梨型身材**

### 案例 3：H型身材

**輸入數據**：
- 胸圍：85 cm
- 腰圍：75 cm
- 臀圍：88 cm
- 肩寬：40 cm

**計算結果**：
- 胸圍-腰圍 = 10 cm ❌（< 15，不符合沙漏型）
- 臀圍-腰圍 = 13 cm ❌（< 20，不符合沙漏型）

**判定結果**：✅ **H型身材**

## 📋 判斷標準對照表

| 身材類型 | 判斷條件 | 修改前 | 修改後 |
|---------|---------|--------|--------|
| **蘋果型** | 腰圍 > 臀圍 | ✅ 相同 | ✅ 相同 |
| **沙漏型** | 胸腰差 + 臀腰差 + 胸臀比例 | 18-20 & 23-25 | **15-25 & 20-30 & 胸臀差<5** |
| **梨型** | 臀圍 > 肩寬×2 + 胸圍 | 臀-肩×2 > 3 | **臀-肩×2 > 5 且 臀 > 胸+3** |
| **倒三角** | 肩寬×2 > 臀圍 | 肩×2-臀 > 3 | **肩×2-臀 > 5** |
| **H型** | 腰部曲線不明顯 | 肩×2-臀 < 3 | **胸-腰 < 15 或 臀-腰 < 20** |

## 🔍 除錯方式

### 1. 打開瀏覽器開發者工具（F12）

### 2. 進入身材分析頁面

### 3. 輸入身體數據

### 4. 查看 Console 日誌

**成功案例（沙漏型）**：
```
🔍 身材判斷數據: {
  胸圍: 90,
  腰圍: 65,
  臀圍: 90,
  肩寬: 40,
  肩寬x2: "80.0",
  胸圍-腰圍: "25.0",
  臀圍-腰圍: "25.0",
  臀圍-肩寬x2: "10.0",
  肩寬x2-臀圍: "-10.0",
  胸臀差: "0.0"
}
✅ 判定為沙漏型
```

**梨型案例**：
```
🔍 身材判斷數據: {
  胸圍: 85,
  腰圍: 68,
  臀圍: 96,
  肩寬: 38,
  肩寬x2: "76.0",
  胸圍-腰圍: "17.0",
  臀圍-腰圍: "28.0",
  臀圍-肩寬x2: "20.0",
  肩寬x2-臀圍: "-20.0",
  胸臀差: "11.0"
}
✅ 判定為梨型
```

## 💡 為什麼這樣修改？

### 1. 沙漏型優先判斷

**原因**：沙漏型身材的特徵是胸臀比例均衡、腰部明顯收窄。如果先判斷梨型，很多沙漏型會被誤判。

**解決**：將沙漏型判斷提前到第2順位（僅次於蘋果型）。

### 2. 放寬沙漏型標準

**原因**：原標準（18-20 & 23-25）過於嚴格，實際上沙漏型身材有更大的變化範圍。

**解決**：
- 胸圍-腰圍：15-25 cm（放寬 ±5 cm）
- 臀圍-腰圍：20-30 cm（放寬 ±5 cm）
- 新增胸臀差 < 5 cm（確保比例均衡）

### 3. 提高梨型門檻

**原因**：原標準（臀-肩×2 > 3）太容易觸發，導致沙漏型被誤判。

**解決**：
- 提高門檻至 5 cm
- 新增條件：臀圍必須明顯大於胸圍（> 3 cm）

### 4. 新增除錯日誌

**原因**：方便開發者和用戶了解判斷依據。

**解決**：在 Console 輸出所有計算數據和判定結果。

## 🎯 修改檔案

**檔案位置**：`src/components/wardrobe/Analysis.jsx`

**修改行數**：第 61-138 行（`getFemaleBodyType` 函數）

## ✅ 測試步驟

1. 重新整理前端頁面
2. 進入「身材分析」頁面
3. 輸入標準沙漏型數據：
   - 胸圍：90 cm
   - 腰圍：65 cm
   - 臀圍：90 cm
   - 肩寬：40 cm
4. 查看判定結果：應該顯示「沙漏型身材」
5. 打開 Console 查看詳細數據

## 📚 參考資料

**沙漏型身材標準**：
- 胸圍和臀圍相近（差異 < 5 cm）
- 腰圍明顯小於胸臀（胸-腰 > 15 cm，臀-腰 > 20 cm）
- 肩寬適中

**梨型身材標準**：
- 臀圍明顯大於胸圍和肩寬
- 下半身較為豐滿
- 上半身相對纖細

---

**修復日期**：2025-12-03  
**修復狀態**：✅ 已完成  
**測試狀態**：待測試
