# InsightFace 模型手動下載步驟

## 問題說明

❌ 模型文件損壞（只有 29 bytes）  
❌ Hugging Face 自動下載需要授權  

## ✅ 解決方案：手動下載（最可靠）

### 方法 1：從 GitHub Release 下載（推薦）

```powershell
# 使用 PowerShell 下載
$url = "https://github.com/facefusion/facefusion-assets/releases/download/models/inswapper_128.onnx"
$dest = "$env:USERPROFILE\.insightface\models\inswapper_128.onnx"

Write-Host "正在下載 inswapper_128.onnx..."
Write-Host "目標: $dest"
Write-Host "大小: 約 550 MB，請耐心等待..."

Invoke-WebRequest -Uri $url -OutFile $dest -TimeoutSec 600

if (Test-Path $dest) {
    $size = (Get-Item $dest).Length / 1MB
    Write-Host "下載完成！文件大小: $([math]::Round($size, 2)) MB"
} else {
    Write-Host "下載失敗"
}
```

### 方法 2：從備用來源下載

如果方法 1 失敗，嘗試這些來源：

1. **ModelScope（國內鏡像）**
   ```
   https://modelscope.cn/models/iic/inswapper_128
   ```

2. **直接從作者的 Google Drive**（如果可用）
   - 搜尋 "inswapper_128.onnx download"
   - 找到可靠的下載連結

### 方法 3：從 Hugging Face 手動下載（需要帳號）

1. 註冊/登入 Hugging Face: https://huggingface.co/
2. 訪問：https://huggingface.co/deepinsight/inswapper
3. 點擊 Files → inswapper_128.onnx
4. 點擊下載按鈕
5. 下載完成後，移動到：
   ```
   C:\Users\Administrator\.insightface\models\inswapper_128.onnx
   ```

---

## 驗證下載

下載完成後，運行：

```powershell
# 檢查文件是否存在
Test-Path "$env:USERPROFILE\.insightface\models\inswapper_128.onnx"

# 檢查文件大小（應該 > 500 MB）
$file = Get-Item "$env:USERPROFILE\.insightface\models\inswapper_128.onnx"
$sizeMB = [math]::Round($file.Length / 1MB, 2)
Write-Host "文件大小: $sizeMB MB"

if ($sizeMB -gt 500) {
    Write-Host "✅ 模型文件正常！"
} else {
    Write-Host "❌ 文件過小，可能下載失敗"
}
```

---

## 重啟後端

模型下載成功後：

1. **停止當前後端**（在運行後端的終端按 `Ctrl+C`）

2. **重新啟動**：
   ```powershell
   cd C:\Users\Administrator\Desktop\SmartClosetAI
   python -m uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
   ```

3. **預期日誌輸出**：
   ```
   ✅ InsightFace 已導入
   開始初始化 FaceSwapService...
   初始化 FaceAnalysis (buffalo_l)...
   ✅ FaceAnalysis 初始化成功
   找到現有模型: C:\Users\Administrator\.insightface\models\inswapper_128.onnx
   ✅ 臉部交換模型載入成功
   ✅ FaceSwapService 初始化成功
   ```

---

## 快速測試（重啟後端後）

在小助手輸入：
```
幫我生成今天的穿搭建議
```

查看日誌應該顯示：
```
🎭 開始第二階段：臉部交換
✅ 臉部交換成功！相似度: 99%
```

---

## 常見問題

### Q: 為什麼不能自動下載？
A: Hugging Face 的這個倉庫需要授權，目前最可靠的方式是手動下載。

### Q: 文件大小應該是多少？
A: 正常的 inswapper_128.onnx 約 **550 MB**（約 550,000,000 bytes）

### Q: 如果下載很慢怎麼辦？
A: 可以嘗試使用下載器（如 IDM）或者從國內鏡像站下載。

---

## 狀態總結

- ✅ InsightFace 0.2.1 已安裝
- ✅ `face_swap.py` 硬編碼禁用已移除
- ❌ 模型文件需要手動下載（舊文件已刪除）
- ⏳ 等待下載完成並重啟後端

完成後，臉部交換功能即可正常使用！
